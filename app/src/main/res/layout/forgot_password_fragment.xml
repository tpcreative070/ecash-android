<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:background="@color/white">

    <TextView
        android:id="@+id/tv_user_name"
        android:layout_width="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:textSize="@dimen/text_normal"
        android:text="@string/str_account_name" />

    <EditText
        android:id="@+id/edt_user_name"
        android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_user_name"
        android:layout_alignParentStart="true"
        android:background="@drawable/edt_bottom_line"
        android:hint="@string/str_input_user_name"
        android:inputType="text"
        android:padding="10dp"
        android:textSize="@dimen/text_normal" />
    <TextView
        android:id="@+id/tv_error_user_name"
        style="@style/TextViewErrorInput"
        android:layout_below="@+id/edt_user_name"
        android:text=""
        />

    <Button
        android:id="@+id/btn_confirm"
        android:layout_below="@+id/tv_error_user_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_border_rectangle_blue"
        android:text="@string/confirm"
        android:textColor="@android:color/white"
        android:textStyle="bold" />

</RelativeLayout>